$v_\pi(s)$: value of state s under policy $\pi$ (expected return)
$\top$: means transpose
$q_\pi(s,a)$: value of taking action a in state s under policy $\pi$
$\stackrel{\cdot}{=}$ : equality relationship that is true by definition
$\mathbb{E}$ :  Expected value of a variable basically
$\in$: Is an element of; e.g., $s\in S$
$V,V_t$: array estimates of state value function $v_\pi$ or $v_*$
$\hat{v}$ : This is so I don't forget how to do the hat lol
The q  is often called a q-value, it typically used to refer to the action-value function
[[CS272]]

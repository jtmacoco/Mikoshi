- Basically we want our model (which is a function essentially) to be more complex rather than just a linear function like y=mx+b
- Basically want to get rid of lines
- How we does this is by using an activation function so we still are using the linear combination but will pass that output from the linear combination through and <span style="color:rgb(192, 0, 0)">activation function</span>
 - Note to much complexity may not be the best

## Hyperparameters
- <span style="color:rgb(0, 176, 80)">Width : </span>number of hidden units per layer
- <span style="color:rgb(0, 176, 80)">Depth</span>: number of hidden layers in the "deepnet"
- Learning Rate
- Batch size
- Momentum
- Decay

## Loss
- Note that the loss depends on the full dataset
- So basically what the loss is saying that this is the average loss of the data set since we want the model to learn the whole data set so it can generalize

[[CS271]]
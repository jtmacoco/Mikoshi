- This is used to basically describe what ML really is but super basica
- The equation is simple : $f(x)=xw+b$ 
	- x is the feature
	- w is the weight/parameter
	- b is the bias
- So now say we wanted to train a model using this linear function this would look![[Linear-Model.excalidraw]]
- This line provides shows that all the values above are x's and below are dots
- So this line generalizes for both features (x's and dots) since it's in the middle of the 2
- The point is to show that the function finds the best position where it's kind of equidistant from the average of all the points.
- This is a good approximation

## Multiple inputs and multiple outputs
$y_1=x_1*w_11+x_2*w_21+b_1$
$y_2=x_1*w_12+x_2*w_22+b_2$
Notice how each output has it's own bias
This is just an example of how the function would look like for multiple inputs and outputs but there is typically many more


[[CS271]]
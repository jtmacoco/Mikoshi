---
title: 994. Rotting Oranges
source: "[[Leetcode]]"
tags:
  - leetcode
  - graph
created: 2026-08-22
difficulty: medium
status: unsolved
---

## Problem

You are given an `m x n` `grid` where each cell can have one of three values:

- `0` representing an empty cell,
- `1` representing a fresh orange, or
- `2` representing a rotten orange.

Every minute, any fresh orange that is **4-directionally adjacent** to a rotten orange becomes rotten.

Return _the minimum number of minutes that must elapse until no cell has a fresh orange_. If _this is impossible, return_ `-1`.

---
# Example 1

![[994. Rotting Oranges-20260822152032737.png]]

**Input:** `grid = [[2,1,1],[1,1,0],[0,1,1]]`

**Output:** 4

---
# Solution

>[!important] Intuition
>Use bfs since want min
>Keep track of fresh oranges and rotten with vars then run bfs on rotten oranges only
>

```python
class Solution(object):
    def orangesRotting(self, grid):
        n = len(grid)
        m = len(grid[0])
        direction = [(0,-1),(0,1),(-1,0),(1,0)]
        rotten = deque()
        fresh = 0
        for row in range(n):
            for col in range(m):
                orange = grid[row][col]
                if orange == 1:
                    fresh += 1
                if orange == 2:
                    rotten.append([row,col,0])
        res = 0
        while rotten:
            r,c,t = rotten.popleft()
            res = max(res,t)
            for dr, dc in direction:
                nr = r+dr
                nc = c+dc
                if nr < 0 or nr >= n or nc < 0 or nc >= m:
                    continue
                if grid[nr][nc] == 1:
                    fresh-=1
                    rotten.append([nr,nc,t+1])
                    grid[nr][nc] = 2
        return res if fresh == 0 else -1

        """
        :type grid: List[List[int]]
        :rtype: int
        """
```

```html-embed
Mikoshi/ICEbreaker/Leetcode/Leetcode_Assets/rotting-oranges-bfs.html
1100
```

---
# Time & Space Complexity

**Time Complexity:** $O(n*m)$ where $n$ is size of rows and $m$ is size of cols
**Space Complexity:** $O(n*m)$ where $n$ is size of rows and $m$ is size of cols

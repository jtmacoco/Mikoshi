---
title: 347. Top K Frequent Elements
source: "[[Leetcode]]"
tags:
  - leetcode
  - hashtable
created: 2026-07-03
difficulty: medium
status: solved
---

## Problem

Given an integer array `nums` and an integer `k`, return _the_ `k` _most frequent elements_. You may return the answer in **any order**.

---
# Example 1:

**Input:** ` nums = [1,1,1,2,2,3], k = 2`

**Output:** `[1,2]`

---
# Solution

>[!important] Intuition
> Use a bucket array to keep track of most frequent, this avoids sorting

## Steps
1. Init lists and dicts
2. collect frequency of each value
3. use frequency count of each value as the index for the bucket, then append the values
4. Loop through each bucket in reverse since index will maintain the most freq

```python
class Solution:
    def topKFrequent(self, nums: List[int], k: int) -> List[int]:
        bucket = [[] for _ in range(len(nums)+1)]
        freq = {}
        res = []
        for n in nums:
            freq[n] = freq.get(n,0)+1
        for key, cnt in freq.items():
            bucket[cnt].append(key)
        for i in range(len(bucket)-1,-1,-1):
            for val in bucket[i]:
                if val in res:
                    continue
                res.append(val)
                if len(res) == k:
                    return res

        return res
```

```cpp
class Solution {
public:
    vector<int> topKFrequent(vector<int>& nums, int k) {
        map<int,int> freq;
        vector<int>res;
        vector<vector<int>>bucket;
        bucket.resize(nums.size()+1);
        for(int i = 0; i < nums.size(); i++){
            freq[nums[i]]++;
        }
        for(const auto &[num,cnt]:freq){
            bucket[cnt].push_back(num);
        }
        for(int i = bucket.size(); --i>=0;){
            for(auto val:bucket[i]){
                auto found = find(res.begin(),res.end(),val);
                if(found==res.end()){
                    res.push_back(val);
                }
                if(res.size() == k){
                    return res;
                }
            }
        }
        return res;
    }
};
```

---
# Time & Space Complexity

**Time Complexity:** $O(n)$
**Space Complexity:** $O(n)$

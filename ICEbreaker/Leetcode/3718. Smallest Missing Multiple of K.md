---
title: 3718. Smallest Missing Multiple of K
source: "[[Leetcode]]"
tags:
  - leetcode
  - hashtable
created: 2026-08-25
difficulty: easy
status: solved
---

## Problem

Given an integer array `nums` and an integer `k`, return the **smallest positive multiple** of `k` that is **missing** from `nums`.

A **multiple** of `k` is any positive integer divisible by `k`.

---
# Example 1

**Input:** `nums = [8,2,3,4,6], k = 2`

**Output:** 10

**Explanation:** The multiples of `k = 2` are 2, 4, 6, 8, 10, 12... and the smallest multiple missing from `nums` is 10.


---
# Solution

>[!important] Intuition
> Calc number all multiples of k then check ranging from smallest to largest if it's in the `nums` list

```python
class Solution:
    def missingMultiple(self, nums: List[int], k: int) -> int:
        mult = []
        total = 0
        for i in range(len(nums)):
            total+=k 
            mult.append(total)
        for num in mult:
            if num not in nums:
                return num
        return total+k
```

---
# Time & Space Complexity

**Time Complexity:** $O(n)$
**Space Complexity:** $O(n$)

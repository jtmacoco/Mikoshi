---
title: 78. Subsets
source: "[[Leetcode]]"
tags:
  - leetcode
  - backtracking
created: 2026-08-04
difficulty: medium
status: solved
---

## Problem

Given an integer array `nums` of **unique** elements, return _all possible_ _(the power set)_.

The solution set **must not** contain duplicate subsets. Return the solution in **any order**.

---
# Example 1

**Input:** `nums = [1,2,3]`

**Output:** `[[],[1],[2],[1,2],[3],[1,3],[2,3],[1,2,3]]`

---
# Solution

>[!important] Intuition
> Binary Decision tree either take value or don't take value

```python
class Solution:
    def subsets(self, nums: List[int]) -> List[List[int]]:
        res = []
        def backTrack(i, cur):
            if i >= len(nums):
                res.append(cur[::])
                return 
            cur.append(nums[i])
            backTrack(i+1,cur)
            cur.pop()
            backTrack(i+1,cur)
            
        backTrack(0,[])
        return res
```

---
# Time & Space Complexity

**Time Complexity:** $O(n*n^2)$
**Space Complexity:** $O(n*n^2)$

Each of the `n` elements has 2 choices (include/exclude), and choices multiply across a recursion, so total subsets = 2 × 2 × ... × 2 (n times) = **2^n**.  
Then, copying each subset into the result costs up to O(n), giving total time/space of **O(n · 2^n)**.

The `n` shows up specifically **because** of `cur[:]`
